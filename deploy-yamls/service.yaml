apiVersion: v1
kind: Service
metadata:
  labels:
    app: tokens-statistics
    environment: Development
  name: tokens-statistics
  namespace: jobs
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: tokens-statistics
  sessionAffinity: None
  type: ClusterIP
